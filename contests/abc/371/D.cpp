#include <bits/stdc++.h>
using namespace std;

int n, s, t;
int a[20], b[20], c[20], d[20];
float ans = 0;
vector<int> g[20];

float dis(float x, float y) {
    return pow(x, 2) + pow(y, 2);
}

int main() {
    cin >> n >> s >> t;
    for (int i = 1; i <= n; i++) {
        cin >>> a[i] >> b[i] >> c[i] >> d[i];
        f[i] = false;
    }

    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            
        }
    }


}