// [prefix: perm]

vector<ll> p(n);
iota(all(p), 0);
// sort(all(a));

ll ans = 0;
do {
    // p[i] は「 i 番目にどの要素を選ぶか」のインデックス
    for (ll i = 0; i < n; i++) {
        // a[p[i]] みたいに使う
    }
    cout << nl;
} while (next_permutation(all(p)));